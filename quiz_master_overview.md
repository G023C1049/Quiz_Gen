# Quiz GēN — アプリ概要・技術資料

## 📱 アプリ概要

**Quiz GēN** は、Google Gemini AI がリアルタイムで問題を生成する日本語クイズアプリです。
毎回プレイするたびに異なる問題が生成されるため、何度でも新鮮な体験が楽しめます。

> [!NOTE]
> バックエンドサーバーは持たず、ブラウザから直接 Gemini API を呼び出す **フロントエンドオンリー** 構成です。

---

## 🎮 主な機能

| 機能 | 内容 |
|------|------|
| **AI問題生成** | Gemini が指定ジャンル・トピックで5問を一括生成 |
| **ジャンル選択** | 雑学・アニメ・映画・歌詞・歴史・観光地の6ジャンル |
| **カスタムトピック** | 好きなトピックを自由入力して問題生成 |
| **段階的回答判定** | AI＋独自ロジックの多段階判定システム |
| **カウントダウンタイマー** | 1問60秒の制限時間 |
| **効果音** | Web Audio API による正解・不正解・タイムアップ音 |
| **ジャンル別統計** | LocalStorage に累計正解数・不正解数を記録 |
| **レーダーチャート** | タイトル画面でジャンル別正答率を可視化 |
| **モデル自動フォールバック** | APIが失敗したら下位モデルに自動切り替え |

---

## 🖥️ 画面フロー

```
タイトル画面
  ↓（スタート）
モード選択画面
  ├─ ジャンルモード → ジャンル選択画面 → 問題生成中画面 → ゲーム画面
  └─ 普通モード（ランダムジャンル）→ 問題生成中画面 → ゲーム画面
                                                              ↓
                                                       リザルト画面
                                                              ↓
                                                       タイトル画面
```

---

## 🌟 アプリの魅力・工夫点

### 1. AIによる無限の問題生成
毎回 Gemini AI が新しい問題を生成するため、問題が尽きません。
カスタムトピックに「推しのアーティスト名」や「好きなゲーム」などを入れれば、世界に一つだけのクイズが作れます。

### 2. 段階的回答判定システム（SmartJudge）
テキスト入力式クイズの難題「表記ゆれ」を4段階で解決しています。

```
段階1: 完全一致チェック（正規化後の文字列比較）
  ↓ 判定できない場合
段階2: キーワードマッチチェック（一致率70%以上で正解）
  ↓ 判定できない場合
段階3: 明らかな不正解チェック（空回答・諦め回答・型不一致）
  ↓ 判定できない場合
段階4: AI判定（Gemini が意味的な同一性を判断）
```

例：「東京都」と入力しても「東京」が正解なら✅ 正解になります。

### 3. レート制限への対応（指数バックオフ）
Gemini API の利用制限（429エラー）に達した場合、
最大30秒まで自動で待機し、リトライする仕組みを実装。
さらに上位モデルが使えない場合は **下位モデルへ自動フォールバック** します。

```
Gemini 2.5 Pro → Gemini 2.5 Flash → Gemini 1.5 Flash
```

### 4. ジャンル別成長可視化
LocalStorage に累計戦績を保存し、タイトル画面に **レーダーチャート** で表示。
どのジャンルが得意・苦手かが一目でわかります。

### 5. Web Audio API 効果音
外部音源ファイル不要。ブラウザ標準の Web Audio API で音を生成しています。

---

## 😓 苦労した点

### 回答判定の精度向上
テキスト入力式は「◯◯（人名）」「○○市」など表記が多様なため、
単純な文字列比較では誤判定が多発。キーワード抽出・正規化・AI判定の組み合わせで解決しました。

### APIキー管理のミス
当初ソースコードに APIキーを直書きしていた。`.env` ファイルへの移行時に：
- Create React App の `REACT_APP_` プレフィックスルールを知らず変数が `undefined` になった
- `.env` のキー末尾に誤字（`_APP`）が混入していた

→ デバッグを重ねて解決。環境変数の正しい扱いを習得。

### AI出力のJSON解析
Gemini の返答は常に正しい JSON 形式とは限らず、
マークダウンのコードブロック（` ```json ``` `）で囲まれることもある。
正規表現で柔軟に JSON を抽出する処理を追加しました。

---

## ⚙️ 技術スタック

### フロントエンド

| 技術 | 用途 |
|------|------|
| **React 19** | UIフレームワーク・状態管理（useState / useEffect） |
| **Create React App** | ビルドツール・開発環境 |
| **Tailwind CSS v3** | スタイリング（ユーティリティクラス） |
| **Chart.js / react-chartjs-2** | タイトル画面のレーダーチャート |
| **Lucide React** | アイコン（Trophy, Clock, Brain など） |
| **Web Audio API** | 効果音生成（外部ファイル不要） |
| **LocalStorage** | ジャンル別統計データの永続化 |

#### 画面コンポーネント構成

```
src/
├── App.jsx                     # ルートコンポーネント・画面管理・ゲームロジック
├── components/
│   ├── TitleScreen.jsx         # タイトル・統計表示・レーダーチャート
│   ├── ModeSelectScreen.jsx    # モード選択（ジャンル / 普通）
│   ├── GenreSelection.jsx      # ジャンル選択・カスタムトピック入力
│   ├── GeneratingScreen.jsx    # 問題生成中のローディング画面
│   ├── GameScreen.jsx          # メインゲーム画面・タイマー・判定UI
│   └── ResultScreen.jsx        # 結果表示・正答率
├── services/
│   ├── enhancedGeminiService.js  # Gemini API呼び出し・一括問題生成・フォールバック
│   ├── smartJudgeService.js      # 段階的回答判定システム
│   └── aiJudgeService.js         # AI判定（Gemini による意味判定）
├── hooks/
│   └── useTimer.js             # タイマーカスタムフック
└── utils/
    ├── gameLogic.js            # ゲームロジックユーティリティ
    └── sounds.js               # 効果音ユーティリティ
```

#### 状態管理（App.jsx）

```javascript
// 主要な state 一覧
currentScreen       // 現在の画面（'title' | 'modeSelect' | 'genreSelection' | 'generating' | 'game' | 'result'）
currentQuestions    // 生成された問題の配列
currentQuestion     // 現在の問題インデックス
score               // 累計スコア（正解 +10点）
timeLeft            // 残り時間（秒）
userGenreStats      // ジャンル別統計（LocalStorage と同期）
```

---

### バックエンド相当（Gemini API連携）

バックエンドサーバーは存在せず、フロントエンドから直接 Google Gemini API を REST で呼び出します。

#### API呼び出しの仕組み

```
ブラウザ
  └─ fetch() でHTTP POST
       ↓
https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent?key={APIKEY}
       ↓
Gemini API（Google Cloud）
       ↓
JSON レスポンス
```

#### 使用モデル（優先順）

| モデル | 用途 |
|--------|------|
| `gemini-2.5-flash` | 第1候補（高速） |
| `gemini-2.5-pro` | 第2候補（高品質） |



#### 問題生成プロンプト設計

- ジャンル・トピックを指定
- **JSON形式のみ** で出力するよう厳命
- 一言で答えられる問題のみを要求
- 事実に基づく内容を要求（ハルシネーション抑制）

#### エラーハンドリング

```
APIエラー発生
  ├─ 429（レート制限）→ 指数バックオフ（最大30秒）でリトライ（最大3回）
  ├─ その他エラー    → 次のモデルへフォールバック
  └─ 全モデル失敗   → ユーザーにエラー表示
```

---

## 🔐 セキュリティ

| 項目 | 状況 |
|------|------|
| APIキー管理 | `.env` ファイルで管理（`REACT_APP_GEMINI_API_KEY`） |
| Gitへの除外 | `.gitignore` に `.env` を追加済み |



---

## 📊 今後の改善案

- [ ] バックエンドサーバーを経由してAPIキーをサーバーサイドで管理
- [ ] 難易度選択機能（easy / medium / hard）
- [ ] 問題の履歴表示・復習モード
- [ ] ランキング機能（オンライン対応）
- [ ] PWA対応（オフラインプレイ）
